{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Windows) Launch",
            "type": "lldb",
            "program": "${workspaceRoot}/target/release/flux",
            "windows": {
                "program": "${workspaceRoot}/target/release/flux.exe",
            },
            "request": "launch",
            "args": [],
            "stopOnEntry": false,
            "cwd": "${workspaceRoot}",
            "env": {
                "RUST_BACKTRACE": "full"
            },
            "terminal": "integrated",
        },
    ]
}